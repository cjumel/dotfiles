# This file defines ignore rules for `fd` (which powers several fuzzy search algorithms I use), in addition to the default ones (e.g. ignored stuff in git repositories)

# Git stuff
.git/

# Virtual environments
.venv/
node_modules/

# Cache directories
.mypy_cache/
.pytest_cache/
.ruff_cache/
__pycache__/

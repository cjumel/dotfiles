local config = {}

config.color_scheme = "Dracula" -- Close to default Wezterm theme, with less aggressive colors & better contrast
config.colors = { background = "#000000" } -- Pure black background color, like in default theme

return config

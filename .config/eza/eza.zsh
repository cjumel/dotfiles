alias ls='eza' # Overide the builtin ls; this will be propagated to other aliases using `ls`

# `-T` is not an option of the builtin `ls` command
alias lt='eza -T'   # List files in directory in a tree-like format
alias lta='eza -Ta' # List files in directory in a tree-like format, including hidden files

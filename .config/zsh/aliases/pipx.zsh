# [P]ip[X]: install and manage Python packages in isolated environments

alias pxi='pipx install'    # [I]nstall: install a package
alias pxil='pipx list'      # [L]ist: list installed packages
alias pxup='pipx upgrade'   # [U][P]grade: upgrade a package
alias pxui='pipx uninstall' # [U]n[I]nstall: uninstall a package

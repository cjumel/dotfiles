alias st='stow --no-folding'          # I only use stow with --no-folding, so let's make it the default
alias sta='stow --no-folding --adopt' # Like `stow --no-folding` but if a file already exists at the symlink target, adopt it with its changes

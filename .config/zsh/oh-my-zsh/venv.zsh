# All these aliases assume we use/create an environment located in .venv/
alias vec='python -m venv .venv'      # Create
alias vea='source .venv/bin/activate' # Activate
alias ved='deactivate'                # Deactivate
alias ver='rm -rf .venv'              # Remove

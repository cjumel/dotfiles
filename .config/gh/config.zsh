# Make available two terminal aliases, as well as other more advanced features:
# - `ghce` to ask Copilot to explain something
# - `ghcs` to ask Copilot to suggest something
eval "$(gh copilot alias -- zsh)"
